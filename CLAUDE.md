# CLAUDE.md — Winterreise

## ⚠️ ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ ПЕРВОЙ ЗАПИСИ ПЕСНИ

**Перед фиксацией перевода новой песни в JSON** — выполнить и привести вывод:

```bash
python3 scripts/pre-translation-check.py <N>
```

Скрипт автоматически (1) grep каждого значимого DE-слова по всем переведённым песням; (2) для NEW слов — поиск семьи по корню (правило 32); (3) выводит lines_de для ручной сверки пунктуации; (4) печатает чеклист дальнейших шагов.

После запуска скрипта — обязательно:

1. **Привести вывод в чат**, прокомментировать каждое совпадение по словарной семье (применить конвенцию или сознательно отступить с сигнализацией).
2. **Сверка пунктуации** каждой DE-строки по ≥3 источникам (LiederNet + Wikisource Müller WH 1824 + schubertsong.uk). **Hampson Foundation не использовать** (опечатки).
3. **Список спорных мест** с конкретными альтернативами (правило 6).
4. **Müller vs Schubert** различия явно перечислить.
5. **Получить одобрение пользователя** по каждому спорному пункту (правило 7).
6. **Только после этого** — записывать перевод в JSON.

**Без выполнения этого протокола первая запись песни ЗАПРЕЩЕНА.** Все ранее обнаруженные провалы (matt в Песне 18, weist в Песне 19, пунктуация L8 Песни 19) были вызваны игнорированием этого протокола.

## ⚠️ ОБЯЗАТЕЛЬНЫЙ ПРОТОКОЛ «ПРОВЕРКА ТОЧНОСТИ ПЕРЕВОДА»

Триггеры: «проверь точность», «исследуй слово», «проверь по словарям», «полное исследование».

**Главный принцип**: Müller — 1820-е, переводим **именно его смыслы** того времени, не современные. Приоритет источников:

1. **Смыслы 1820-х** (Adelung Wien 1811, Grimm DWB) — **ГЛАВНОЕ**. Если Adelung даёт `thöricht = unsinnig, toll` (старое), а DWDS modern даёт `unvernünftig, dumm` (современное) — у Müller'а **старое**.
2. **Контекст оригинала и цикла** — главное наравне со словарями 1820-х. Тема цикла (например, безумие героя в Winterreise) задаёт регистр.
3. **DWDS + Pfeifer** — современное значение и этимология (вспомогательно).
4. **Duden + Duden Synonyme** — синонимическое поле (вспомогательно).
5. **БНРС, Multitran DE→RU, PONS, Langenscheidt** — RU-кандидаты (вспомогательно).
6. **Обратная сверка RU→DE** (БРНС, Multitran RU→DE) — **ТОЛЬКО ПРОВЕРКА**, что мы случайно не ушли слишком далеко. **Не аргумент в пользу выбора** — современные словари не покрывают смыслы 1820-х; смещение RU→DE может быть «в правильную сторону» по Adelung'у.
7. **RU поэт. традиция XIX в.** (Пушкин-Лермонтов и т.п.) — **в последнюю очередь**, и **только при явном цитировании с указанием источника**. Без верификации не использовать (склонность к галлюцинациям).

**Процедура**:
- **Шаг 0 (ОБЯЗАТЕЛЬНЫЙ, форсинг-функция)**: `python3 scripts/word-check.py <DE-слово> [RU-кандидат...]` — параллельно опрашивает все 14 источников (Adelung BSB, Grimm DWB, DWDS, Pfeifer, Duden, Duden Synonyme, Wiktionary DE, Wortschatz Leipzig, БНРС, Multitran DE→RU, PONS, Langenscheidt + Multitran/БРНС RU→DE). **Полный вывод привести в чат**. Для ⚠ источников (SPA/JS-load: DWDS, Pfeifer, Duden, Duden Syn, PONS, Langenscheidt, Leipzig) — обязательно WebFetch URL вручную, вывод также в чат. Без вывода скрипта + ⚠ WebFetch'ей заявлять «исследование проведено» ЗАПРЕЩЕНО.
- **Шаг 1**: Анализ собранного: Adelung + Grimm → смыслы 1820-х; DWDS/Duden/Wiktionary → современное; БНРС/Multitran/PONS/Langenscheidt → RU-кандидаты.
- **Шаг 2**: Контекст (оригинала + цикла) — приоритет над любым отдельным словарём (правило 4). Поэт. компаунды (Felsengrund, Wüstenei) имеют расширенное контекстуальное значение.
- **Шаг 3** (контрольный): обратная RU→DE сверка — не sanity check, что RU-кандидат не сместился катастрофически. **Не основной аргумент** — современные словари не покрывают 1820-е.

Пример: `törichtes` — Adelung 1811: «**ehedem unsinnig, toll**»; контекст цикла: безумие сквозное → «безумное», не «безрассудное».

**Без выполнения Шагов 0-3 заявлять «исследование проведено» ЗАПРЕЩЕНО.** Дублируется в правилах 21, 31 и разделе «Словари».

## Project Overview

Подстрочный перевод вокального цикла «Зимний путь» (Winterreise) Мюллера / Шуберта. 24 песни, немецкий + русский с пословной разметкой и аннотациями. Vue 3 + Vite, GitHub Pages.

## Architecture

### Страница

- **Левая колонка**: немецкий оригинал (строфами)
- **Правая колонка**: русский перевод; под каждым словом мелким шрифтом — соответствующее немецкое
- **Сноски** `¹²³` у отмеченных фрагментов; сквозная нумерация в пределах песни. Наведение — подсветка фрагмента, клик — пояснение
- **Режим пояснений** внизу страницы — все аннотации песни
- **Меню**: 24 песни (немецкие названия)
- **Заголовок песни**: немецкое + русское название
- Поиск, переключатель темы, печать/PDF

### Админ-режим

URL `?admin=<SECRET_KEY>`. Inline-редактирование, сохранение через GitHub Contents API (токен в localStorage, права `repo`). Параллельно — правка JSON в Claude Code CLI.

### Data Model

JSON по одной песне в `src/data/songs/`:

```json
{
  "number": 2,
  "title_de": "Die Wetterfahne",
  "title_ru": "Флюгер",
  "stanzas": [{
    "lines_de": ["Der Wind spielt mit der Wetterfahne"],
    "lines_ru": [{
      "segments": [
        { "ru": "Ветер", "de": "Der Wind" },
        { "ru": "играет", "de": "spielt" },
        { "ru": "с", "de": "mit" },
        { "ru": "флюгером", "de": "der Wetterfahne" }
      ],
      "annotations": [
        { "type": "lang", "segment_range": [3, 3], "text": "..." }
      ]
    }]
  }]
}
```

**Правила данных:**
- `segments` — пары {ru, de} в порядке русского
- `de` может быть пустой строкой
- `segment_range` — индексы [start, end] включительно (подсветка)
- `lines_ru.length` == `lines_de.length`. Verbklammer/рамка через границу строк — одним сегментом-рамкой в одной из lines_ru (см. правило 16)
- Строфы — по источнику (Müller, Waldhornist 1824); каждая авторская строфа — отдельный объект в `stanzas`, не сваливать в `stanzas[0]`
- `title_annotations`: массив `{ type, text }` для комментариев к песне в целом; без `segment_range`

### Tech Stack & Structure

Vue 3 (Composition API, `<script setup>`) + Vite + чистый CSS + plain JS. Деплой GitHub Actions → GitHub Pages. `src/data/songs/*.json` (01-24), `src/data/index.json` (метаданные), `src/components/` (SongList, SongView, InterlinearLine, FootnoteMark, AnnotationsPanel + `admin/`), `src/styles/`, `src/utils/`.

## Development Guidelines

- Интерфейс ТОЛЬКО на русском
- Минималистичный дизайн (десктоп; мобильная версия позже)
- **Никогда не менять структуру данных ради верстки.** Не умещается строка — чинить CSS.

## Translation Rules

**Правила ниже — обобщения опыта, не аксиомы.** При противоречии правила и фактической DE/RU грамматики приоритет имеет грамматика. Если правило даёт грамматически некорректный RU — правило неприменимо в этом случае; решать по грамматике.

Подстрочник — пословное соответствие, не литературный перевод.

**Точность — приоритет, не поэтика.** Русский грамотный и реально существующий (проверять сочетаемость: «лежит на душе» — есть, «лежит на уме» — нет), но **не обязан звучать красиво**. Точное лучше привычного: `блестящих`, не `светлых`; `в споре`, не `наперебой`. **При равной точности** — частотный вариант. **Устоявшийся русский перевод не аргумент** держаться менее точного. Не добавлять отсутствующих оттенков: уменьшительных суффиксов, ложных архаизмов, аспектных значений (`взглянуть` мгновенно ≠ `sehn` нейтрально). **Поэтически переводим только если оригинал был поэтичен** в немецком 1820-х (Adelung-помета `gehoben`/`dichterisch`/`veraltet 1820-х`, Grimm `poetisch`). Нейтральное DE → нейтральное RU; высокое DE → высокое RU. Когда оригинал поэтический, точность ВКЛЮЧАЕТ поэтичность.

**Многословный/дефисный перевод обязателен, если точнее.** Если одно RU слово неточно, а два слова или компаунд через дефис передают значение точно — делать многословно/дефисно. Не сжимать ради краткости. Примеры: `Verlangen → «страстное стремление»` (а не урезанное «желание»); `Täuschung → «обман-иллюзия»` (правило 45). Это применимо к любой части речи. См. также правило 45.

**Перевод DE → RU. Английский не использовать** ни в обсуждениях, ни в скобках, кроме исключительных случаев.

**Порядок работы.** Для КАЖДОГО знаменательного слова (включая частицы и наречия: `immerfort` = непрестанно; `je` = хоть когда-нибудь):

1. **DWDS** (значения, регистр, этимология). Для XIX в. также **Grimm** (dwds.de/wb/dwb/СЛОВО) и **Adelung Wien 1811** (BSB: `lexika.digitale-sammlungen.de/adelung/suche/abfrage?lemma=СЛОВО` → ссылка `/adelung/lemma/bsbXXXX` → текст). **`zeno.org` недоступен** (TCP timeout с большинства провайдеров) — использовать BSB-зеркало. Регистр и значения дрейфуют (`starren` 1820-х → «застыть»; сейчас «уставиться»).
2. **Разобрать на приставку + корень** (Er-sterben ≠ sterben, zer-schmelzen ≠ schmelzen).
3. **Близкие слова развести** (Boden/Erde, Gras/Rasen, Blüte/Blumen).
4. **Контекст важнее этимологии и словаря, точность важнее традиции.** При двух одновременных значениях (`starrt` = застыл + уставился) — отметить оба. **Частицы** (schon, doch, ja, auch, wohl, je, noch, mal, denn) меняют значение по контексту: `Schon` в `Tret' ich auch schon` — усилитель уступки; `denn` в вопросе — «же»; `Doch` в `Doch wieder zu finden` — эмфатическое «всё же», не противительное «однако». **Модальные в значении предположения** (`mag` от `mögen` 3 = «должно быть») — отдельный RU. **Поэт. компаунды имеют расширенное контекстуальное значение**: Felsengrund по DWDS = Felstal (скальная долина), но в Песне 9 контекст (tiefsten + Ausgang finden) → «скальные ущелья» (узкое замкнутое место); Wüstenei по словарю = Einöde, но по Grimm включает felsige Berglandschaften. Двунаправленная словарная сверка (протокол в начале файла) не заменяет контекстуальный анализ.
5. **Русская пунктуация.** Запятые при обращениях, вводных, деепричастиях. **Не калькировать DE-пунктуацию автоматически**: `Nun, ...` (DE — запятая) → «Теперь не далеко уже идти» (RU — без запятой). **При замене слова перепроверить согласование**: «своё отражение» → «свой образ» (м.р.).
6. **Если выбор неочевиден — сигнализировать**, не делать молча. Идиомы, архаизмы, многозначные слова, расхождение регистра — выносить на обсуждение.

**Источник DE: шубертовская редакция** (как поют), не оригинал Мюллера. Источник: Deutsche Schubert-Lied-Edition (Naxos). Орфография по реформе 1996: ß после кратких → ss (Muß → Muss); после долгих остаётся (Blumenstrauß).

1. **Каждое DE-слово — в подстрочнике.** Не пропускай служебные (da, schon, doch). Слово с самостоятельным RU — отдельный сегмент даже в рамке (`hinaus → прочь`, `nach → вслед`). Если RU-предлог уже передаёт направление приставки — поглощается: `In die Felsengründe lockte... hin → «В глуби заманил»`. **Знаменательные слова (модальные, частицы, наречия) обязаны иметь RU-эквивалент**: `mögen` 3 = «должно быть» — не растворять в инфинитиве. **Прозрачность подстрочника**: даже когда DE-RU словарь даёт компактный перевод (`sich hingeben → предаваться`), предпочитать разделённый/буквальный (`Gibt... sich hin → «отдаёт... себя»`), чтобы каждое DE-слово было видно отдельно.

2. **По умолчанию сохраняй DE порядок слов** — чтобы видеть соответствие. Перестройка — только когда русский **нечитаем**, а не «слегка непривычен». **Поэтическая инверсия V-letzt** (`Von der Straße her ein Posthorn klingt`) сохраняется: «С улицы почтовый рожок доносится». **Поэтическая инверсия Gen впереди** (`meiner Hoffnung Grab`, `des ganzen Winters Eis`) тоже сохраняется в RU: «На моей надежды могиле», «Всей зимы лёд» — поэтически читается. Распрямлять (Gen после) только при реальной нечитаемости. **Постпозитивная аппозиция прилагательных** (`Der Winter kalt und wild`) — поэт. оборот; прилагательные **без склонения** (синтаксически эквивалент предикативному ist). RU сохраняет: «Зима холодная и буйная».

3. **Меняй порядок только когда русский нечитаем.** «На моей прекрасной любимой доме» — нельзя; «Тут я подумал уже в своём ослеплении» — ок.

4. **Одно русское = одно немецкое (по возможности).** Группировать только когда иначе невозможно. Отделяемые приставки с ясным русским — отдельный сегмент (`dahin` → «вдаль»). Рамка с многоточием (`pfiff'...aus`) — когда русский не разделить. **Многоточие — только при разрыве словами в DE**; рядом стоящие (Folge nach, Komm her, Schaue nach) — без многоточия. **Порядок в рамке = текстовый порядок DE**.

5. **`de` пустая** — для слов, добавленных для русского смысла без DE-эквивалента.

6. **Liebchen = любимая** (не «милая»). Lieb- от Liebe.

7. **Не писать содержательные изменения без приглашения. Не пушить без явного «пушим»/«делаем».** Одобрение перед записью в файл (не только перед commit; файл сразу виден на dev-сервере). Порядок: предложить → «ок» → писать → показать → «пушим» → commit/push. **Аннотации «от себя», `variant_de`/`variant_ru`, ретроактивные правки — каждый случай отдельно.** Одобрение конкретного шага не распространяется на сопутствующие изменения и правила CLAUDE.md «заодно». **Условное одобрение** («если всё чётко, пушим»; «проверь и сделай») — сначала сообщить результат проверки, только потом действовать.

8. **В аннотациях переводи немецкие фрагменты.** «(es eher bemerken — это раньше заметить)», не просто «(es eher bemerken)».

9. **Артикли (ein, der, die, das) ≠ «некий/этот».** Артикль всегда **слева** от своего существительного — соответственно, объединять со **СЛЕДУЮЩИМ** сегментом (существительным или прилагательным справа): `{ "ru": "верной", "de": "ein treues" }`; либо пустой `ru`: `{ "ru": "", "de": "ein" }`. **НЕ группировать с предшествующим словом**, даже когда они образуют устойчивую фразу. Пример: `Welch ein törichtes Verlangen` → `Welch` + `ein törichtes` + `Verlangen` (правильно), не `Welch ein` + `törichtes` + `Verlangen` (ошибка: ein примыкает к Welch, а не к прилагательному).

10. **Предлоги — отдельные сегменты.** `{ "ru": "с", "de": "mit" }` + `{ "ru": "сердцами", "de": "den Herzen" }`.

11. **Связка sein → «—» отдельным сегментом, всегда** (bin/bist/ist/sind/seid). Не «есть»/«является». Применяется к любому именному сказуемому: сущ., прил. (полное/краткое), местоимение, adjektives Partizip (статив; модификатор `wie`/`manche Stunde`). **Рамка `Adj. + sein-форма` запрещена**. Тире — RU-стандарт между подлежащим и предикативом, даже при DE Verbletzt-Stellung. Пример: `wie müd' ich bin` → «усталый / — / я,» (Песня 10). **Не применяется** к: (а) Perfekt от глаголов изменения (правило 33); (б) Funktionsverbgefüge (правило 66). **Прошедшее war / Konjunktiv II wär'** требуют RU-глагола.

12. **Идиомы**: буквальный перевод предпочтительнее, если грамматичен (`im Streit` → «в споре»). По смыслу — когда буквальный невозможен (`Was fragen sie nach` → «Что за дело им до»). **Ложные соответствия**: `sich zur Ruhe legen` = просто лечь, а RU «лечь на покой» = умереть. **Сдвиг подлежащего**: `ich hatte X` → «у меня была X»; `was hat es?` → «что с ним?». **Все случаи — с языковой аннотацией.**

13. **Косвенная речь.** В русском нет согласования времён. После «подумал, что...» одновременное действие → настоящее: «подумал, что он освистывает».

14. **Возвратное притяжательное — только в пределах одной RU-клаузы.** Когда DE-посессив (mein/dein/sein/ihr) согласован с субъектом своей клаузы → RU «свой» (для любого лица). **При разбиении на две клаузы** (AcI через «как»/«:», подчинительная, новое предложение) — возвратное «свой» через границу клаузы не работает: антецедент извне передаётся **личным местоимением «его/её/их»**. Пример AcI (правило 40): `Mein Herz sieht ... sein eignes Bild` → «Моё сердце видит: на небе нарисован **его** собственный образ» (две клаузы; «свой» в L10 невозможен — субъект L10 = «образ»).

15. **Род адаптируется.** Sie (Wetterfahne ж.р.) → он (флюгер м.р.). При значимом расхождении — языковая аннотация.

16. **Verbklammer.** Aux + Partizip II = рамка-многоточие: `bin... eingezogen` → «пришёл». Aux + Inf (Modal-Klammer, Futur): `hätt'... sollen`, `wird... gewinnen`. Русское подлежащее перед содержательным глаголом, как в немецком. Содержательный глагол стоит в конце DE — в RU тоже в конец. **НЕ Verbklammer**: стандартный порядок Nebensatz S-Inf-V-fin (`was ich zittern kann` — субъект+инфинитив+финитный модальный — стандартное придаточное, не рамка). Ошибочное оформление S-V-V как рамки ведёт к потере отдельных слов.

17. **Предлог, поглощённый падежом.** `gehüllt in Schnee` → «окутан снегом» — `{ "ru": "снегом", "de": "in Schnee" }` (творительный поглощает `in`).

18. **Никаких пустых сегментов (`ru = ""`).** DE-слово без RU — сливается с соседним в `de` (рамка). **Пустой `de`** (при непустом `ru`) — для RU-добавок для грамматики: «что» для dass, «Если/Когда» для V1-условий, «как» для AcI (правило 40), «Тот» как восстановленный антецедент `der`, **RU-предлог при DE Dat./Akk. без предлога** (DE `mir` → RU «ко мне» / «от меня» / «у меня» через пустой de). Перед добавлением — проверить, не решается ли пунктуацией. **Vorfeld-`es`, контракции `'s` = es** (формальный субъект/катафорический объект) — сливаются с глаголом в рамке: `'s führt → Ведёт`, `seh's → вижу`. **Различение `'s`**: перед глаголом = `es` (поглощается); перед сущ. ср.р. или после предлога = `das` (артикль, правило 9: `An's Haus, 's Meer`).

19. **Модальные глаголы в контексте.** Sollen во 2 л. (du sollst) — воля говорящего: `Sollst meinen Tritt nicht hören` → «Моих шагов не услышишь».

20. **Эллипсис глагола.** Немецкий допускает опущение, русский — нет: `Sacht, sacht die Türe zu!` → «Прикрою тихо-тихо дверь». Обязательна языковая аннотация.

21. **Не угадывать — проверять** (см. раздел «Словари» ниже). **Приоритет — смыслы 1820-х**: Adelung Wien 1811 + Grimm DWB (главные). DWDS modern и БНРС — вспомогательно (могут смягчать/искажать). Если Adelung даёт старое значение, отличное от DWDS modern — у Müller'а **старое**. **Близкие RU-синонимы развести по корням**: трепетать (DE zittern) ≠ трепаться (DE flattern). **RU→DE обратная сверка — только sanity check, не аргумент**: современные словари не покрывают 1820-е, смещение может быть «в правильную сторону». **При запросе «проверить точность» — Шаг 0: `python3 scripts/word-check.py <слово> [RU-кандидаты...]` обязателен**, вывод привести в чат (см. протокол в начале CLAUDE.md). Без скрипта «исследование» не считается выполненным.

22. **Schubert vs Müller — двойная сверка** (LiederNet + schubertsong.uk + NSA; Hampson не использовать — опечатки). Сноска в одном источнике → доверять. Алгоритм атрибуции — правило 57. При первой итерации — список всех различий (орфография, элизии, артикли, словопорядок) с цитатами **до** записи перевода. Проверять модификаторы (hier und da, noch, so) в полном контексте. `variant_de`/`variant_ru` — только после одобрения каждого случая.

23. **Элизии (апостроф)** — последовательно в `lines_de`, `title_de`, `de` сегментов. Примеры: zieh', dacht', hätt', hab', Wär', Ruh', pfiff', Gefror'ne, An's. Перед многоточием рамки апостроф сохраняется (`pfiff'... aus`).

24. **Перевод через отрицание при расхождении коннотаций.** `lau` (акцент на «недостаточно горячий») → «негорячи», не «теплы» (рус. «тёплый» позитивно).

25. **`So` в `so...dass` — всегда степень.** «так»/«настолько», никогда «едва/еле». Направление задаётся следствием в dass-придаточном.

26. **Konjunktiv II ≠ настоящее.** Hätte/wäre/wollte — прошедшее + «бы»: wolltet → «хотели бы». После «словно/как будто» — прошедшее.

27. **Шубертовские музыкальные повторы — через ` / `** в `lines_de`, `de`/`ru`: `"Und / Ihr dringt..."`, `{ "ru": "А / Вы", "de": "Und / Ihr" }`. Первый — основной, второй — повтор.

28. **Одно DE через цикл — один RU; близкие в одной песне — разные.** Конвенции: `rauschen → шуметь`, `Geselle → дружище`. Близкие (Boden/Erde, Flur/Wiese/Feld; кластеры Schmerz/Leid/Leiden) — разные RU. Каузативные пары: `dringen aus → вырываться`, `durchdringen → прорвать`, `drängen → рваться`. **Модификаторы блокируют конвенцию**: so/noch/je/gar — проверять. **Substantiv vs attributiv**: `manches Blatt` (attr.) → «не один лист» vs `manches` (subst.) → «всякое». **Морф. пары в одной песне** (Wegweiser/Weiser → Путевой указатель/указатель; Bächlein/Bach) — различать; правило 28 — к одному слову через цикл, не к формам корня внутри песни. Контекстуальное отступление — с сигнализацией (правило 6).

29. **Регистр двунаправленно.** Возвышенное DE → возвышенное RU; нейтральное → нейтральное. Не «украшать» поэтизмами обычное у Müller'а. Проверять DWDS (gehoben, dichterisch, veraltet) + Adelung 1811 + Grimm: DWDS `veraltet` отражает современный язык, в 1820-х могло быть стандартным (`ward`). Русские ловушки без основания: «око», «знать», «грезить», «вещать».

30. **Предлог или направительное наречие глагольной конструкции — в рамку с глаголом.** `suchen nach`, `fragen nach` → `{ "ru": "ищу", "de": "such'... nach" }`. То же для `her`, `hin`, `herauf`, `hinaus`, грамматически примыкающих к глаголу: `Von der Straße her ein Posthorn klingt` → `her... klingt` идёт с глаголом, `Von` — отдельный предлог.

31. **Грамматические утверждения проверять** (см. раздел «Словари»): DWDS, Duden, E-VALBU (IDS Mannheim, https://grammis.ids-mannheim.de/verbvalenz). Первая ассоциация может быть ложной. **При запросе «проверить точность» — Шаг 0 `scripts/word-check.py` + двунаправленная сверка обязательны** (см. протокол в начале CLAUDE.md и правило 21).

32. **Глагольные приставки несут смысл.** Zer- = разрушение (zerbrechen). Er- = завершение процесса (ersterben — угаснуть, erstarren — оцепенеть). Ver- = изменение/утрата. Не переводить только корень: zerschmelzen ≠ schmelzen.

33. **Sein + Partizip II от глаголов изменения = Перфект, не связка.** «Die Blumen sind erstorben» = цветы угасли (Perfekt). Правило 11 (sind → тире) не применяется. Признак: причастие от глагола изменения (ersterben, einziehen, aufstehen). **Модификатор `wie` блокирует Perfekt-чтение**: `Mein Herz ist wie erstorben` ≠ `Mein Herz ist erstorben`. С `wie` Partizip II — **адъективное причастие** (статив, сравнение): `ist wie erstorben` → «есть словно угасшее» (связка через тире). Аналогично `wie tot`, `wie betrunken`. Правило 33 применяется к Aux+Partizip II **без модификаторов** `wie/als ob`.

34. **Futurisches Präsens.** Wenn + Präsens → RU **будущее**: `Wenn die Gräser sprossen wollen` = когда захотят прорасти. **V1 + явный Nom без wenn/als** — почти всегда **бессоюзное условие** или Folgesatz после императива; в RU «Если/Когда/И» с пустым `de` (правило 18). Пример: `Spielt der Wind mit meinem Blatte` → «Когда играет ветер...». Исключение: V1 как ответ на вопрос по рифмовке. Проверять **каждое** V1 + явный субъект.

35. **Спектр значений: абстрактное и предметное.** Контекст определяет. `Angedenken` = и воспоминание, и сувенир — `mitnehmen` указывает на предметное. `Fluss` ≠ автоматически «река»: общее слово; контекст (прозрачная, замерзает, поэт сам называет Bach) → «речка». **Предлоги тоже многозначны**: `Durch` = «через» (поперёк) или «по» (вдоль) — `Durch des Bergstroms trockne Rinnen` = вдоль русел.

36. **Cross-distribution: смыслы распределены по-разному.** Если DE-конструкция и RU-эквивалент делят смысл не параллельно — не писать ложных пар. Пример: `Ob's... wohl` ↔ «Уж не... ли» (`ob` = «не...ли», `wohl` = «уж»). Решение: вся DE-конструкция в рамку с многоточием, лишняя RU-частица — пустой `de`. Сопровождать языковой аннотацией.

37. **Связность ландшафтной сцены.** Проверять согласованность отдельных слов с общей картиной. «Скальные лощины» (пологие) ≠ «горная река» + «вниз» — один неточен.

38. **Классические/библейские аллюзии — сохранять узнаваемость.** `Jeder Strom wird's Meer gewinnen` = Экклезиаст. «Каждая река обретёт море» узнаваемо; «каждый поток» теряет.

39. **`erst` в `nun erst`, `jetzt erst`, `in... erst` — временной контраст, не первичность.** «лишь»/«только» (= не раньше, только сейчас), **не «впервые»**. `erst` относится к обстоятельству, не к объекту.

40. **AcI (Acc + Inf/Partizip) в RU невозможен.** Передавать через: (а) **«как»-клаузу** (`Fühlst deinen Wurm... sich regen` → «Чувствуешь, как твой червь встрепенётся»); или (б) **двоеточие** (`Einen Weiser seh' ich stehen` → «Один указатель вижу я: стоит») — стандарт RU после глаголов восприятия. **Предпочесть (б)** если требуется меньше пустых de-сегментов (правило 18); «как»+«он» — это уже 2 добавочных RU-слова. **Обе формы создают новую клаузу** → «свой» через границу не работает (правило 14).

41. **Возвратные русские глаголы + `Mit` — сохранять «с».** `Mit + Dat + sich-verb`: `Mit` = comitative/manner, не чистый инструменталь. RU возвратные (встрепенуться, обернуться) **не принимают чистый творительный** — «С горячим жалом встрепенётся».

42. **Не приписывать оригиналу переклички из перевода.** Если RU создал параллель (`unwirtbarem` → «бесприютном» и `Obdach` → «приют»), проверить DE: разные корни? Если да — параллели у Müller'а нет.

43. **Перед переводом значимого слова — grep по всем песням.** `grep '"de": "<слово>"' src/data/songs/*.json`. Либо следовать конвенции (правило 28), либо сознательно отступить с сигнализацией. **Также — другие морф. формы корня в этой же песне** (Wegweiser↔Weiser): различать (правило 28).

44. **Избегать русских конструкций с поверхностной омографией.** «так, как» vs «так как»; «что бы» vs «чтобы»; «то же» vs «тоже»; «также» vs «так же». Для `so wie` — «Такими, какими…», «Как…», «Подобно тому, как…». Если фраза двусмысленна на беглом восприятии — переформулировать.

45. **Компаунды через дефис для непереводимых корневых семей или многослойных значений.** RU не имеет однословного эквивалента: даём 2 или 3+ компонентов через дефис. Применять когда: (а) многозначность важна; (б) ни один RU-синоним не покрывает всё; (в) контекст требует всей палитры. Все компоненты склоняются согласованно. Применимо к любой части речи: сущ. (`Traum → сон-мечта`, `Täuschung → обман-иллюзия`), глаг. (`erlaben → освежать-услаждать`), прил. (`elend → жалок-несчастен-обездолен` — 3 компонента: jämmerlich + bedrückt + этимол. `ali-lanti`).

46. **«Дословно»/«буквально» vs «прямой перевод».** «Дословно X — Y» требует **корневого + грамматически параллельного** соответствия (Apfel → яблоко). Когда корни/морфология не совпадают, но Y — однословный непосредственный эквивалент → **«прямой перевод»** (Träumer → прямой перевод «сновидец»). При частичном покрытии — оговорка («с потерей смыслов», «этимологически», «в одном из значений»).

47. **В lang-аннотациях цитировать фактическую форму строки, не лемму.** Лемма указывается отдельно (`Глагол nachdenken — ...`), не атрибутируется Müller'у. **Перед оформлением стандартной lang-аннотации** (отрицание, sollen, AcI, эллипсис) — посмотреть как в других песнях того же типа и единообразить `segment_range` (обычно узкий, на DE-слово) и формулировку. **Внутри одной аннотации** все RU-формы одного DE-слова — единообразны (не «Путевой указатель» в начале и «Путевой столб» в конце).

48. **Структурные утверждения о цикле проверять чтением песен.** «X — последнее появление Y», «впервые…» — конкретный текст может опровергнуть. Проверка: grep + чтение релевантных песен. **Различать формы**: `lassen` и `verlassen` — два разных факта. **Конкретные перекрёстные ссылки** программно проверять (Python: пройти по annotations и собрать пересечения), а не вспоминать. **Утверждения об исторических изданиях — по нескольким независимым источникам** (Wikipedia EN/DE, Youens, Reed).

49. **Творительный образа действия — слово действия/манеры, не часть тела** (дополнение к 17). DE `mit + Adj + N` для образа действия → русские «X шагом/поступью/походкой» (ходьба), «X голосом/взглядом» (речь/взгляд). НЕ «X ногой/стопой» для ходьбы. Пример: `mit trägem Fuß` → «вялым шагом».

50. **Dass-Ausrufesatz: `Ach, dass... so [adj]!` — Klage** (сетование, не удивление; к 1820-м архаично). Перевод: «Ах, что [Subj] так [Adj]!» + усилительная «же». Структурно: dass→что, so→так; «же» — пустой `de`. Пример: `Ach, dass die Luft so ruhig!` → «Ах, что же воздух так спокоен!» (Песня 12).

51. **Шубертовские прямые повторы внутри строки** (дополнение к 27). Schubert часто повторяет слова в вокальной партии. `Lines_de` отражает sung-text. Проверять партитуру (IMSLP, NSA). **Повтор как ресурс перевода**: одно DE слово, повторённое дважды, — два слота для разных RU пластов: `so elend, so elend` → «так жалок, так несчастен» (jämmerlich + unhappy).

52. **Регистровый переворот за 200 лет** (дополнение к 29). Регистр некоторых слов изменился между 1820-ми и современностью. Пример: `hell` — Adelung `edler`; `licht` — `«im gemeinen Leben»`. Современный DWDS наоборот. **Доверять Adelung 1811 для регистра 1820-х**.

53. **Adelung'овские градации значимы** (дополнение к 21). «erste Grad», «mit Widerwillen», «weniger als…» меняют RU: `trübe` (erste Grad der Unlust) → «сумрачный», не «мрачный»; `träge` (mit Widerwillen) → «вялый»; `säumen` → «мешкать»; `matt` → «слабый»; `elend` → компаунд «жалок-несчастен-обездолен» (правило 45). Для значимого слова — Adelung + Grimm DWB, не только DWDS modern.

54. **Русская типографика в аннотациях, заголовках, `ru`-полях:** «...» ёлочки (не `"..."` ASCII); — длинное тире (не `-`, дефис только в компаундах); **ё** обязательна где требуется (особенно в косвенных падежах местоимений и формах глаголов на -ёт: ещё/всё/её/идёт/найдёт/чётко/своё/нём); одинарный пробел; многоточие `...`; **«не что иное, как»** (раздельно, конкретизация) vs «ничто иное» (слитно, отрицание). **Обязательная программная ё-проверка после каждой записи** (regex по типовым ошибкам: `\bнем\b|\bеще\b|задает|осознается|передает|\bидет\b|найдет|\bчетко\b|твердой|замерзш|\bвсем\b|\bсвое\b`).

55. **Следственное `dass` без явного `so`/`derart` в главной части** (литературная архаика XIX в.) → русский следственный союз «**так что**». Без якоря русское «что» читается как комплементное. Контраст: `so lau, dass…` (явный `so`) → «столь… что…»; `Hab' wieder schwarze Haare, dass mir's… graut` (без `so`) → «У меня снова чёрные волосы, **так что** мне… страшно».

56. **Сигнализация о подменах предметов / концептов** (дополнение к 6). Если RU-эквивалент означает иной материальный предмет, чем DE (`Bahre` = открытые погребальные носилки vs «гроб» = закрытый ящик-Sarg) — **обязательно сигнализировать пользователю при первоначальном переводе** до записи в файл. Особенно важно при социально-историческом подтексте.

57. **Три источника Müller'а** (контекст для 22): (1) **Urania 1823** — 12 стихов; (2) **Deutsche Blätter 1823** — 10 «продолжений» (Krähe, Mut, Wegweiser, Leiermann); (3) **Waldhornist II 1824** — все 24 с правками. Schubert: Часть I (1-12) по Urania, Часть II (13-24) по Waldhornist; Deutsche Blätter в Вену 1827 не доходил. **Алгоритм** (DB/WH/S): DB=WH=S, отличие у S → правка Schubert'а; DB≠WH=S → правка Müller'а; DB=S≠WH невозможно. Применим только к 10 «продолжениям». Для Urania и Waldhornist'а без DB — расхождение = правка Schubert'а. LiederNet / schubertsong.uk помечают вариант как «Müller» — часто Deutsche Blätter.

58. **Каждая стихотворная строка — с прописной.** При сегментации первого слова легко получить строчную (если по русской пунктуации не должно быть прописной). **Стихотворная конвенция выше синтаксической**: каждая строка `lines_ru` начинается с прописной. Проверка после записи: первый сегмент каждой строки.

59. **Грамматическая модальность в аннотациях.** Точно сохранять модальность DE: V1 + `?` → «спрашивает», не «просит»; императив (`lass...!`) → «просит / велит»; декларатив → «говорит». Не растворять разные модальности в общем «герой обращается».

60. **`mit` без явного Dat — наречие сопричастности**, не предлог: «тоже»/«вместе»/«заодно», НЕ «с + местоимение». Пример: `Fall' ich selber mit zu Boden` → «падаю я сам тоже на землю». Различие: `mit ihm` (с ним) — предлог; `mit` без объекта — наречие.

61. **Сверка пунктуации DE с авторитетным источником.** При первоначальном переводе и финальной проверке — сверять пунктуацию каждой строки DE с Wikisource (Müller Waldhornist 1824 / Urania 1823), schubertsong.uk (sung-text), LiederNet. Особенно: запятая vs точка с запятой vs точка vs восклицательный знак. Программная проверка: вывести все lines_de и построчно сверить с источником.

62. **Winterreise — психологический цикл, не натуралистический.** Природные приметы **не всегда согласованы реалистически** между песнями: `bunte` листья (Песня 16) vs замёрзшие речки и слёзы (Песни 3, 6, 7) — противоречие, разрешаемое в символическом плане (Youens, Wikipedia). Müller не следует строгой природной хронологии. Не делать необоснованных утверждений о «глубокой зиме» / «поздней осени» на основании одной приметы — учитывать **все** приметы.

63. **Финальная систематическая проверка перед закрытием песни.** Обязательно:
- (а) каждый сегмент DE↔RU проверить пословно;
- (б) каждое фактическое утверждение в аннотациях факт-чекнуть (отсылки на песни — grep; DE-цитаты — корректность);
- (в) программная ё-проверка (правило 54);
- (г) проверка ASCII-кавычек `re.findall(r'"[А-Яа-яёЁ]')`;
- (д) проверка коротких дефисов между RU-словами;
- (е) капитализация первых сегментов строк (правило 58);
- (ж) JSON-валидация;
- (з) сверка пунктуации DE с источником (правило 61);
- (и) **список спорных мест** (вариант X vs Y с обоснованием выбора) явно показать пользователю до объявления песни финализированной.

Без этой проверки песня не считается финализированной.

64. **Futurperfekt через Präsens Perfekt + временное наречие будущего.** Aux+Partizip II + `morgen` / `bis morgen` / `übermorgen` = vollendete Zukunft (разговорный эквивалент Futur II). В RU — **будущее время**, не Präteritum. Признак: семантически прошедшее при наречии будущего нелогично. Пример: `Und morgen früh ist alles zerflossen` → «И завтра утром всё **растечётся**» (не «растеклось»).

65. **Tun-Periphrase (`tun + Inf` в Volkslied).** `tun` — вспомогательный (не «делать»), несёт habituell-эмфатику (постоянное действие). RU аналог — народная частица **«знай»** (Ожегов: «постоянно, не переставая»). Сегментация: tun → «знай» отдельным сегментом, смысловой Inf — в конце RU (правило 16). Пример: `Tun sich... erlaben` → «Знай, себя... услаждают».

66. **Funktionsverbgefüge — устойчивые DE-сочетания.** `offen lassen`, `liegen lassen`, `zu Ende sein mit` и т.п. — переводить **идиоматически одним RU-глаголом**, не покомпонентно. Сегментация: рамка без многоточия (DE рядом) или с многоточием (при разрыве).

## Словари — справочник по типам и доступу

`<W>` ниже — слово в URL (для DE кириллица не нужна, для RU использовать UTF-8 percent-encoded). Все доступны через curl/WebFetch, если не указано иное.

### A. DE толковые исторические (для Müller'а 1820-х) — приоритет для регистра

| Словарь | Год | URL | Доступ | Авторитет |
|---|---|---|---|---|
| **Adelung Wien 1811** | 1811 | **BSB**: 2 шага — (1) поиск `lexika.digitale-sammlungen.de/adelung/suche/abfrage?lemma=<W>` (umlauts: `%F6`/`%FC`); (2) из результата `href="../lemma/bsbXXX_Y_Z_NNN"` → `lexika.digitale-sammlungen.de/adelung/lemma/bsbXXX_Y_Z_NNN`. ⚠ `zeno.org/Adelung-1793` — недоступен (TCP timeout) | ✅ через BSB | **МАКС для 1820-х** |
| **Grimm DWB** | 1854-1971 | `dwds.de/wb/dwb/<W>` (HTML сложный — для извлечения **WebFetch** надёжнее curl+regex) | ✅ | **МАКС исторический** |
| **Campe** | 1807-1811 | BSB IIIF Manifest: тт. 1-5 = `bsb10523280, 10523276, 10523282, 10523283, 10523284`; сканы JPG, не текст | ⚠ только как изображения | **ВЫСОКИЙ для 1820-х** (резерв, когда Adelung+Grimm недостаточно) |
| **Pierer 1857, Meyers 1905, Brockhaus 1911** | XIX-XX | все на `zeno.org` — **сейчас недоступен** (TCP timeout) | ❌ | средний — начало XX |
| **Köbler mhd** | совр. | `koeblergerhard.de/mhd/mhd_<буква>.html` (огромный индекс по букве, `iconv` нужен) | ✅ | для средневерхненемецких форм (Müller иногда архаизмы) |

### B. DE толковые современные

| Словарь | URL | Доступ | Авторитет |
|---|---|---|---|
| **DWDS** | `dwds.de/wb/<W>` | ✅ | **МАКС академический** (ИБА Берлин) |
| **DWDS Etymologie (Pfeifer)** | `dwds.de/wb/etymwb/<W>` | ✅ | МАКС этимология |
| **Duden** | `duden.de/rechtschreibung/<W>` | ✅ | МАКС школьный |
| **Duden Synonyme** | `duden.de/synonyme/<W>` | ✅ | синонимическое поле |
| **Wiktionary DE** | `de.wiktionary.org/wiki/<W>` | ✅ | для обзора |
| **etymonline** (EN, на основе Kluge) | `etymonline.com/search?q=<W>` | ✅ | альтернатива Kluge (paywall) |
| **Wortschatz Leipzig** | `corpora.uni-leipzig.de/de/res?corpusId=deu_news_2024&word=<W>` | ✅ | корпус (частоты/кокурренции) |

### C. DE→RU

| Словарь | URL | Доступ | Авторитет |
|---|---|---|---|
| **БНРС (Лейн/Москальская)** | `translate.academic.ru/<W>/de/ru/` | ✅ | **МАКС академический** |
| **Multitran** | `multitran.com/m.exe?l1=3&l2=2&s=<W>` | ✅ | огромный объём, смешан |
| **PONS DE-RU** | `de.pons.com/%C3%BCbersetzung/deutsch-russisch/<W>` | ✅ | надёжный |
| **Langenscheidt DE-RU** | `de.langenscheidt.com/deutsch-russisch/<W>` | ✅ | авторитетный |

### D. RU→DE

| Словарь | URL | Доступ | Авторитет |
|---|---|---|---|
| **БРНС (Цвиллинг)** | `translate.academic.ru/<W>/ru/de/` | ✅ | **МАКС академический** |
| **Multitran RU-DE** | `multitran.com/m.exe?l1=2&l2=3&s=<W>` | ✅ | большой объём |

### Заблокированы / недоступны
- **zeno.org** — TCP timeout с этого хоста: вычеркивает Adelung-1793, Pierer-1857, Meyers-1905, Brockhaus-1911. **Для Adelung** — использовать BSB-зеркало (см. выше). Для Pierer/Meyers/Brockhaus альтернатив пока нет.
- **Reverso Context**, **bab.la**, **Linguee DE-RU** — антибот 403 или RU не поддерживается.
- **Sanders 1860, Wahrig онлайн** — нет общедоступной версии.
- **Kluge** — paywall (используй Pfeifer + etymonline).
- **Lexer mhd** — SPA (используй Köbler).

### Рекомендуемый минимум для значимого DE-слова (по убыванию приоритета)

1. **Adelung Wien 1811 (BSB)** (регистр 1820-х) → 2. **Grimm DWB** (исторический спектр) → 3. **DWDS** + **Pfeifer** (современное + этимология) → 4. **Duden** + **Duden Synonyme** (синонимы) → 5. **БНРС** (главный DE→RU) → 6. **Multitran**, **PONS**, **Langenscheidt** DE-RU (резервные) → 7. **БРНС** (обратная сверка RU→DE).

### Процедура «проверка точности перевода»

См. **протокол в начале файла** (Шаги 0, 1, 2, 3). **Шаг 0 (форсинг)**: `python3 scripts/word-check.py <слово> [RU-кандидат...]` — параллельно опрашивает все 14 источников, печатает таблицу, помечает ⚠ SPA/JS-load для обязательного WebFetch вручную. Здесь — workarounds:
- 404/пустой ответ — скрипт сам пробует 1820-е орфо (Tot-→Todt-, rt-→rth-, T-→Th-). Если не сработало — ручной альт. (thöricht/töricht, Wüstenei/Wüste).
- HTML SPA / parsing не справился — скрипт пометит ⚠, тогда **WebFetch** URL вручную.
- Источник недоступен — альтернатива (BSB вместо zeno.org; web.archive.org). Фиксировать новый URL в разделе «Словари».

## Annotation Guidelines

Два типа: `lang` (грамматика — синий) и `meaning` (смысл — коричневый). Нумерация независимая.

**Lang** — Verbklammer, порядок слов, падежи, род, отрицание, идиомы, составные слова.  
**Meaning** — этимология и связи; смена лица; мотивы; многозначность; соц. контекст; Schubert vs Müller с эффектом.

**Стиль:** перед написанием — литературоведы DE/EN; не категорично; без доказательств не утверждать; **не ссылаться на учёных по именам**; **не упоминать словари по именам** (не «по DWDS», «по Adelung'у»); регистровые пометы не цитировать буквально. Источники — для фактчека, не для текста. **Писать о тексте, не об учебнике** — никакой лингв. терминологии типа «Konjunktiv II в немецком выражает...». Каждый факт проверять (правила 31, 47).

### Технические правила аннотаций

- `line_span: N` — охватывает N строк. Сноска в конце последней. **Может пересекать границы строф.**
- `continuation_ranges: [[start, end], ...]` — диапазоны для каждой последующей строки. **`[-1, -1]` пропускает строку без подсветки** (выборочная подсветка через строки — Песня 19 L2 ann про расщепление героя). Не использовать `null`.
- `segment_range: [start, end]` — индексы включительно.
- Аннотации могут перекрываться (один сегмент — и lang, и meaning).
- В тексте `\n` — перенос строки / разделитель абзацев (Песни 12, 14, 19).
- **Ударение в RU** — combining acute accent U+0301 после ударной гласной: `во́рон` (Песня 11), `что́` (Песня 19).

### Формат сегментов

- Отделяемые приставки: `"de": "pfiff'... aus"` (с многоточием при разрыве)
- Рамки предлогов: `"de": "Auf... Haus"`
- Составные слова: одним сегментом в `de`, можно разбить в `ru`: `{ "ru": "женщины образ.", "de": "Frauenbild" }`
- Пустой `ru` — артикль без эквивалента
- Пустой `de` — RU слово, добавленное для грамматики
- **Пунктуация только в `ru`, не в `de`.** `{ "ru": "ах,", "de": "ach" }`.
- **`variant_de` / `variant_ru` — расхождения Schubert vs Müller** (правила 22, 57). Основное `de`/`ru` — Schubert sung-text; `variant_de` — **только различающееся слово** (артикль/предлог общий — в основном de): `{ "de": "den Wegen", "variant_de": "Straßen" }` (Песня 20). Если RU не различается — `variant_ru` не нужен.

## Admin Authentication

- URL: `https://<user>.github.io/winterreise/?admin=<SECRET_KEY>`
- SECRET_KEY произвольная, задаётся при первой настройке
- GitHub Personal Access Token (права `repo`) хранится в localStorage
